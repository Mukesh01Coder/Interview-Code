#include<bits/stdc++.h>
using namespace std;
void swap(int &x, int &y){
    int temp = x;
        x = y;
        y = temp;
}
int main(){
    int n;cin>>n;
    int a[n];
    for(int i=0;i<n;i++) cin>>a[i];

    for(int i=0;i<n;i++){
        int flag = 0;
        for(int j=0;j<n;j++){
            if(a[j]>a[j+1]) {
                swap(a[j],a[j+1]); 
                flag = 1;
            }
        }
        if(flag == 0) break;
    }
    for(int i=0;i<n;i++) cout<<a[i]<<" ";
    cout<<endl;
    return 0;
}
