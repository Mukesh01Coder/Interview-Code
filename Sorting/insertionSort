#include<bits/stdc++.h>
using namespace std;
int main(){
    int n; cin>>n;
    int a[n];
    for(int i=0;i<n;i++) cin>>a[i];

    for(int key=1;key<n;key++){
           //int key = a[s];
           int curr = key-1;
        while(curr>=0 && a[curr]>a[key]) {
            a[curr+1] = a[curr];
            curr--;  
        } 
        a[curr+1] = a[key];
   }    
    for(int i=0;i<n;i++) cout<<a[i]<<" ";
    cout<<endl;
 return 0;
}
