#include<bits/stdc++.h>
using namespace std;
/*void reverse(const string& str){
    int n = str.size();
    if(n==1){
        cout<<str<<endl;
    }else{
        cout<<str[n-1];
        reverse(str.substr(0,n-1));
    }
}*/
int main(){
    string str;
    cin>>str;
   int n = str.size();
   // Method 1
    reverse(str.begin(),str.end());
   for(int i=0;str[i]!='\0';i++) cout<<str[i];

  //Method 2
  /* 
    for(int i=n-1; i>=0; i--) cout<<str[i];
   
    cout<<endl;*/
  //Method 3
  /*
    stack<char>s;
   for(int i=0;i<n;i++) {
       s.push(str[i]);
   }
   
     for(int i=0;i<n;i++) {
      str[i] = s.top();
      s.pop();
   }

   for(int i=0; i<n;i++) cout<<str[i];
   cout<<endl;*/

   //Mehtod 6 using recursion
  // reverse(str);

  
    return 0;
}
